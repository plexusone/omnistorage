# OmniStorage v0.3.0

*Released: June 20, 2026*

## Highlights

- Redis KVS backend for production caching and state management
- Session storage with backend-agnostic server-side session management

## Overview

This release adds Redis support to the KVS layer and introduces a comprehensive session storage system. Sessions support multiple backends (in-memory for development, KVS-backed for production), size limits, JSON validation, and observability hooks.

## Added

### Redis KVS Backend

Production-ready Redis backend for key-value storage:

```go
import "github.com/plexusone/omnistorage/kvs/redis"

store, _ := redis.New(redis.Config{Addr: "localhost:6379"})
store.Set(ctx, "cache:key", data, time.Hour)
```

### Session Storage

Backend-agnostic server-side session management with multiple backends:

#### In-Memory Sessions (Development)

```go
import (
    "github.com/plexusone/omnistorage/session"
    "github.com/plexusone/omnistorage/session/backend/memory"
)

store := memory.New()
sess := session.NewSession("user-123")
store.Save(ctx, sess)
```

#### KVS-Backed Sessions (Production)

Use any KVS backend (Redis, SQLite) for persistent sessions:

```go
import (
    "github.com/plexusone/omnistorage/session"
    "github.com/plexusone/omnistorage/session/backend/kvs"
    "github.com/plexusone/omnistorage/kvs/redis"
)

redisStore, _ := redis.New(redis.DefaultConfig())
sessionStore := kvs.New(redisStore)

sess := session.NewSession("user-123")
sessionStore.Save(ctx, sess)
```

#### Session Controls

Add size limits and violation handling:

```go
import "github.com/plexusone/omnistorage/session/backend/memory"

store := memory.NewWithControls(
    session.WithControls(session.Config{
        MaxSessionSize: 1 << 20, // 1MB limit
        MaxSessions:    1000,
    }),
    session.WithViolationHandler(func(e session.ViolationEvent) {
        log.Printf("session violation: %s", e.Type)
    }),
)
```

## Documentation

- README updated with Redis KVS and Session Storage sections

## Migration

No breaking changes. Existing code using v0.2.0 will work without modification.

## Full Changelog

See [CHANGELOG.md](../../CHANGELOG.md) for detailed commit history.
